apiVersion: v1
kind: Service
metadata:
  name: {{ include "paidiverpy-gui.fullname" . }}
  labels:
    {{- include "paidiverpy-gui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "paidiverpy-gui.selectorLabels" . | nindent 4 }}
