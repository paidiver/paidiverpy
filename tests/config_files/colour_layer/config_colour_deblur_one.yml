general:
  sample_data: "plankton_csv"
  output_path: "./output"
  n_jobs: 1
  image_open_args: "BMP"

steps:
  - colour:
      name: "colour_correction"
      mode: "deblur"
  - colour:
      name: "colour_correction"
      mode: "deblur"
      params:
        method: "wiener"
        psf_type: "gaussian"
        sigma: 10
        angle: 20
  - colour:
      name: "colour_correction"
      mode: "deblur"
      params:
        method: "wiener"
        psf_type: "motion"
        sigma: 10
        angle: 20
  - colour:
      name: "colour_correction"
      mode: "deblur"
      params:
        method: "other_method"
