codecov:
  require_ci_to_pass: yes

coverage:
  status:
    project:
      default:
        # Require 80% coverage, i.e., always succeed
        target: 80
    patch: false
    changes: false

comment: off
